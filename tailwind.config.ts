const config = {
  theme: {
    extend: {
      colors: {
        splash: {
          bg: "var(--bg)",
          ink: "var(--ink)",
          teal: "var(--teal)",
          coral: "var(--coral)",
          slate: "var(--slate)",
          gold: "var(--gold)",
          surface: "var(--surface)",
          line: "var(--line)",
        },
      },
    },
  },
};

module.exports = config;
